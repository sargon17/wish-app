{
  "name": "wish-app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun scripts/vp.mjs run -r build",
    "check": "bun scripts/vp.mjs run -r check",
    "check-types": "bun scripts/vp.mjs run -r check-types",
    "convex:dev": "bun scripts/vp.mjs run -r convex:dev",
    "dev": "bun scripts/vp.mjs run wish-start#dev",
    "dev:all": "bun scripts/vp.mjs run -r dev",
    "dev:doc": "bun scripts/vp.mjs run wish-app-docs#dev",
    "format": "bun scripts/vp.mjs run -r fmt",
    "lint": "bun scripts/vp.mjs run -r lint",
    "sandcastle": "bash -lc 'export GH_TOKEN=${GH_TOKEN:-$(gh auth token)}; bun .sandcastle/main.mts'",
    "sandcastle:build-image": "docker build -t sandcastle:wish-app .sandcastle",
    "sandcastle:lead-delivery": "bash -lc 'export GH_TOKEN=${GH_TOKEN:-$(gh auth token)}; bun .sandcastle/lead-delivery/main.mts'",
    "test": "bun scripts/vp.mjs run -r test"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.5.10",
    "@types/node": "^22.19.15",
    "typescript": "^5.9.3",
    "vite-plus": "^0.1.12"
  },
  "dependencies": {
    "type-fest": "^5.4.4"
  },
  "overrides": {
    "vite": "npm:@voidzero-dev/vite-plus-core@0.1.12",
    "vitest": "npm:@voidzero-dev/vite-plus-test@0.1.12",
    "esbuild": "0.27.1"
  },
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
